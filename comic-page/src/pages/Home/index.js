import React from "react";
import MenuCard from "../../components/MenuCard/MenuCard";

const index = () => {
  return (
    <div>
      <MenuCard />
      <content />
    </div>
  );
};

export default index;
